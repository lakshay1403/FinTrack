# FinTrack
FinTrack is a full-stack expense tracking application built with the MERN stack. It follows MVC architecture on the backend for clean and scalable code. The frontend uses React, Redux, and React Query for state management and data fetching. Users can add, manage, and analyze expenses through a responsive and intuitive interface.
